import Foundation

/// Text and icon weights used outside the dial's rings. Fully opaque is the
/// default and needs no token.
public enum UsageOpacity {
    public static let secondary: Double = 0.55
    public static let inactive: Double = 0.3
}
