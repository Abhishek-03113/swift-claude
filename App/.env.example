# Copy to App/.env and fill in your own values. App/.env is gitignored.
#
# Apple Developer Team ID (Membership tab at developer.apple.com/account).
# Used by App/project.yml via XcodeGen's ${VAR} substitution.
ORBIT_TEAM_ID=
